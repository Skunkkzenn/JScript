let preco = 2.39;
let desconto = 0.4;
let precoFinal = 0.6;
// OU
// let precoFinal = (preco * (1-desconto));


console.log(preco * precoFinal);

//OU

console.log(preco * (1 - desconto));

let nome = "Caderno";
let categ = "Papelaria";
console.log("Produto: " + nome + ", Categoria: " + categ + ", Desconto: " + desconto + ", Preço: " + preco);