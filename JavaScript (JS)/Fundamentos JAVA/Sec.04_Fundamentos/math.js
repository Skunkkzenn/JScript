const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Math.pow(raio, 2) = Raio elevado ao quadrado
                                          // Math.PI = Valor de PI

console.log(area);
console.log(typeof Math);